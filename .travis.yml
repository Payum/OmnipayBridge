language: php

php:
  - 5.5
  - 5.6
  - hhvm

sudo: false

matrix:
  allow_failures:
    - php: hhvm

before_script:
  - php composer.phar --prefer-stable install

notifications:
  email:
    - crevillo@gmail.com
